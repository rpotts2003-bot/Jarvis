# Windows-friendly voice stack (binary wheels — no pipwin/js2py)
SpeechRecognition>=3.10
pyttsx3>=2.90
sounddevice>=0.4.6
numpy>=1.24
edge-tts>=6.1
# Whisper optional later; Google STT works online without it
# edge-tts neural UK male needs internet; offline falls back to Windows SAPI
